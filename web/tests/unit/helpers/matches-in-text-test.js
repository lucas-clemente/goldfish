import {
  matchesInText
} from '../../../helpers/matches-in-text';
import { module, test } from 'qunit';

module('MatchesInTextHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = matchesInText(42);
  assert.ok(result);
});
