import { momentCalendar } from '../../../helpers/moment-calendar';
import { module, test } from 'qunit';

module('Unit | Helper | moment calendar');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = momentCalendar(42);
  assert.ok(result);
});
